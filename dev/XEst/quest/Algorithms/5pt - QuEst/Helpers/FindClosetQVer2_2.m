function [qOut, mIdx] = FindClosetQVer2_2(qRef, qIn)

err = QuatError(qRef, qIn);

[~,mIdx] = min(abs(err));

qOut = qIn(:,mIdx);

end





























































